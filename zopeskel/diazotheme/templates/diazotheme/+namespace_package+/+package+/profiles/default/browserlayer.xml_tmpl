<?xml version="1.0"?>
<!-- Register the package-specific browser layer,
     so that it will be activated
     when this product is installed. -->
<layers>
    <layer name="${namespace_package}.${package}.customization.layer"
      interface="${namespace_package}.${package}.interfaces.ICustomTheme" />
</layers>
